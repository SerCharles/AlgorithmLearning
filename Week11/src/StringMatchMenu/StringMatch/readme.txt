首先，输入字符只能是0-9这十个数字，或者大小写英文字母，或者英文的小中大括号，逗号，句号，分号，减号，否则会被抛出异常或者输出错误结果。
其次，从文件读入的文件只能是.txt格式，要求第一行为以一个空格隔开的两个数字n，m，分别代表待比较字符串和模式串的长度，两者必须为正整数，而且分别不能超过100000000，1000。第二行是一个长度为n的串，也就是待比较字符串。第三行是个长度为m的串，也就是模式串。
最后，在界面内输入只需要输入字符串和模式串即可。